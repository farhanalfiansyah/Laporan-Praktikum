#include <stdio.h>

int main()
{   int array[100],i,n;
    array[0]=0;
    array[1]=1;
    printf("Masukkan nilai n: ");scanf("%d",&n);
    printf("%d %d",array[0],array[1]);

    for(i=2;i<=n-1;i++)
        {array[i]=array[i-1]+array[i-2];
        printf(" %d",array[i]);}
}
