#include <stdio.h>

int main()
{   int n,i,A[100];
    printf("Jumlah data: ");scanf("%d",&n);
    for(i=0;i<n;i++)
        {printf("Masukkan data ke %d: ",i+1);
        scanf("%d",&A[i]);}
    for(i=0;i<n;i++)
    {printf("\nData ke %d adalah:%d ",i+1,A[i]);}
}
